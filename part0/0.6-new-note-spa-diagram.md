sequenceDiagram
    participant browser
    participant server
    
    Note right of browser: User types "888" and clicks Save
    Note right of browser: JS captures form submit event
    Note right of browser: JS adds note to local array
    Note right of browser: JS re-renders note list locally
    
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note left of server: Server saves the new note
    activate server
    server-->>browser: {"message": "note created"}
    deactivate server
    
    Note right of browser: Note already visible 
